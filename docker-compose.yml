services:
  cgvwatch:
    build: .
    container_name: cgvwatch
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      TZ: Asia/Seoul
    volumes:
      - ./data:/data
